# Moosh Personal

- Every shipped feature is available without a license, trial, grace period, free pass, or checkout.
- The app contains no activation, deactivation, license revalidation, account, pricing, or upgrade UI.
- The product name is `Moosh` and the bundle identifier is `com.tyrival.moosh`.
- Release builds target `arm64` and use one stable self-signed code-signing identity.
- Sparkle trusts only the Moosh EdDSA public key and reads `https://tyrival.github.io/alt-tab-macos/appcast.xml`.
- Release assets come only from `https://github.com/tyrival/alt-tab-macos/releases/`.
- The fork remains GPL-3.0 and identifies itself as an unofficial modified version.
